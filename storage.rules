rules_version = '2';

// Grant access to authenticated users
service firebase.storage {
  match /b/{bucket}/o {
    // Match any file path
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
